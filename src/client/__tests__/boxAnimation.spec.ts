jest.unmock('../boxAnimation');
describe('Datagrid', function () {
    it('should set header with correct label for each field, plus an empty header for actions', function () {
        expect(1 + 1).toEqual(2);
    });
});